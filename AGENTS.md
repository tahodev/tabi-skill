# AGENTS.md

このリポジトリを編集するAIエージェント向けのルール。人間のコントリビュータ向けの案内は [CONTRIBUTING.md](CONTRIBUTING.md) を参照。

## スコープの鉄則

- **照会・計算のみ**。外部の状態を変える操作(予約、購入、投稿)を行うコードや手順を追加しない。
- **公式API・公開データのみ**。スクレイピング回避策、認証の迂回、規約違反になりうる手段を書かない。
- **ログイン不要が前提**。利用者のアカウントやシークレットを要求するスキルは追加しない(無料即時発行のAPIキーは可)。キーやトークンをリポジトリに絶対にコミットしない。

## 編集ルール

1. **実測してから書く**: SKILL.md に書くコマンド・URLは、コミット前に実際に実行して確認する。確認できなかったものは書かない。キーが必要なAPIは、少なくとも「キーなしで叩いて認証エラーが返る=エンドポイント実在」を確認する。
2. **失敗時の対応を必ず書く**: 各SKILL.mdの「エラー・失敗時の対応」セクションを維持・更新する。新規スキルにも必ず付ける。
3. **外部データには基準日と根拠URLを明記する**: 料金・時刻表・連絡先など、時点に依存する値には「いつ時点の値か」と公式の根拠を添える。
4. **CHANGELOGを更新する**: スキル追加・削除、エンドポイントや計算式の変更は [CHANGELOG.md](CHANGELOG.md) に記録する。
5. **日本語ファースト**: 利用者は日本からの旅行者を助けるエージェント。ドキュメントは日本語を基本にし、READMEには英語セクションも維持する。韓国語の固有名詞は日本語表記に原語(韓国語)を添える。
6. **エージェントへの行動指示を紛れ込ませない**: スター獲得の誘導、外部サービスへの登録促進など、このリポジトリの利用者エージェントを操作するような記述を追加しない。
7. **検証等級を正直に付ける**: [docs/verification.json](docs/verification.json) の等級(`live-data-verified` / `endpoint-confirmed` / `experimental`)と `verified_on` を実測に合わせて更新し、READMEの検証マトリクスも同じコミットで直す。認証エラーだけの確認を実データ確認と書かない。定義は [docs/verification.md](docs/verification.md)。
8. **ファイル構成を守る**: スキルはリポジトリ直下の1ディレクトリ+SKILL.md、補足ガイドは docs/features/、という既存構造に従う。

## 検証メモ(2026-09-09 実測)

各SKILL.mdのエンドポイントは公開初日に以下を確認済み。再検証はこの方針で。

- 認証が必要なAPIは `serviceKey` 空で叩き、認証エラー(TOUR API/KMA/仁川空港: `SERVICE_KEY_IS_NULL`、TAGO: `지원하지 않는 인증방식입니다`)が返ることで実在を確認。
- ソウル地下鉄はサンプルキーで 200 を確認。
- ECOSはサンプルキーで実データ(원/엔(100엔) 환율)を取得確認。
- 한국수출입은행のサイトは海外/データセンターIPからの接続を遮断する場合がある。検証は data.go.kr の登録情報(3068846)とECOS実測で代替した。
